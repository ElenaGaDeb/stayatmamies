<%= simple_form_for([ profile, review ], remote: true) do |f| %>
  <%= f.input :content, label: "My review", placeholder: '25 characters minimum' %>
  <%= f.input :rating, label: "My rating", placeholder: '0 to 5' %>
  <%= f.submit "submit my review", class: "btn btn-primary" %>
<% end %>
